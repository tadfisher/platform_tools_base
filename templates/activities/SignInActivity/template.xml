<?xml version="1.0"?>
<template
    format="4"
    revision="5"
    name="Sign-In Activity"
    description="Creates a new login activity, allowing users to optionally sign in with Google+ or enter an email address and password to log in to or register with your application."
    minApi="8"
    minBuildApi="14">

    <dependency name="android-support-v4" revision="8" />

    <category value="Activity" />

    <parameter
        id="activityClass"
        name="Activity Name"
        type="string"
        constraints="class|unique|nonempty"
        default="SignInActivity"
        help="The name of the activity class to create" />

    <parameter
        id="layoutName"
        name="Layout Name"
        type="string"
        constraints="layout|unique|nonempty"
        suggest="${activityToLayout(activityClass)}"
        default="activity_signin"
        help="The name of the layout to create for the activity" />

    <parameter
        id="activityTitle"
        name="Title"
        type="string"
        constraints="nonempty"
        default="Sign in"
        help="The name of the activity." />

    <parameter
        id="parentActivityClass"
        name="Hierarchical Parent"
        type="string"
        constraints="activity|exists|empty"
        default=""
        help="The hierarchical parent activity, used to provide a default implementation for the 'Up' button" />

    <parameter
        id="packageName"
        name="Package name"
        type="string"
        constraints="package"
        default="com.mycompany.myapp" />

    <parameter
        id="includePlus"
        name="Enable Google+ Sign-in and APIs"
        type="boolean"
        default="true" />

    <parameter
        id="includeGames"
        name="Enable Google Play Games Services"
        type="boolean"
        default="false">
    </parameter>

    <parameter
        id="includeDrive"
        name="Enable Google Drive APIs"
        type="boolean"
        default="false">
    </parameter>

    <parameter
        id="includeWallet"
        name="Enable Google Wallet Instant Buy APIs"
        type="boolean"
        default="false">
    </parameter>

    <thumbs>
        <thumb>template_signin_activity.png</thumb>
    </thumbs>

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
