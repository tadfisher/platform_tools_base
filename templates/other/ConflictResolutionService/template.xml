<?xml version="1.0"?>
<template
    format="4"
    revision="2"
    name="CloudSave ConflictResolutionService"
    description="Creates a new service to resolve conflicts caused when cloud save entities are updated in more than one place.">

    <category value="Google" />

    <parameter
        id="className"
        name="Class Name"
        type="string"
        constraints="class|unique|nonempty"
        default="ConflictResolutionService" />

    <parameter
        id="isExported"
        name="Exported"
        type="boolean"
        default="true"
        help="Whether or not components of other applications can invoke the service or interact with it" />

    <parameter
        id="isEnabled"
        name="Enabled"
        type="boolean"
        default="true"
        help="Whether or not the service can be instantiated by the system" />

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
